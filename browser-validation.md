# 浏览器连续导航验证记录

本轮改造将公开网页的默认打开模式设为兼容阅读。页面由服务端读取后提取至多 80 个可导航链接，并由当前标签的历史栈管理前进与后退；用户仍可主动切换到直连网页模式。

2026-08-17 的实际预览验证显示，`https://news.ycombinator.com` 能在内置浏览器中列出公开新闻链接；点击一条新闻后，当前标签地址更新为 `https://simonwillison.net/2026/Aug/16/qwen-38-27b/`，文章内容与其后续链接保持在同一标签中。随后通过工具栏“后退”回到 Hacker News 首页，再通过“前进”恢复同一篇 Simon Willison 文章；两次恢复均在同一个内置标签内完成。

同一预览中，`https://www.bilibili.com` 的首页读取到公开视频卡片。服务端将 BV 视频地址旁的播放量标签替换为对应封面标题；点击首个视频后，当前标签地址更新为 `https://www.bilibili.com/video/BV1VGb86mEDg` 并进入内置视频播放模式。使用“返回上一页”后，页面回到 B 站兼容阅读首页而非外部浏览器。

第三方站点仍可通过登录、反爬、人机验证、DRM、`X-Frame-Options` 或 CSP 限制内容获取与嵌入；这些限制无法也不会被绕过。界面现明确提示此边界，并在可能时保留当前标签的公开阅读与链接导航入口。

验证命令：`pnpm check && pnpm test && pnpm build`。结果：5 个测试文件、19 项测试通过，生产构建通过。
